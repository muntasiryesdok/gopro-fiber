version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gopro-fiber
    ports:
      - 5000:5000
    networks:
      - dev-network
    volumes:
      - ./:/app

networks:
  dev-network:
    driver: bridge
